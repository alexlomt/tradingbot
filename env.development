# Copy .env.example and modify for development environment
NODE_ENV=development
RPC_ENDPOINT=https://api.devnet.solana.com
WS_ENDPOINT=wss://api.devnet.solana.com
CLUSTER=devnet
# ... (other development-specific settings)
